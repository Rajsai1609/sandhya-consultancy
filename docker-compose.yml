version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: sandhyait_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: sandhyait
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
